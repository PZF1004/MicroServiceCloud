server: 
  port: 3344 
  
spring:
  application:
    name:  MicroServiceCloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:PZF1004/MicroServiceCloud.git #GitHub上面的git仓库名字
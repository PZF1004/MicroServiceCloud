spring:
  cloud:
    config:
      name: MicroServiceCloud-config-client #需要从github上读取的资源名称，注意没有yml后缀名
      label: master   
      profile: dev   #本次访问的配置项
      uri: http://config-3344.com:3344  
#本微服务启动后连接MicroServiceCloud-config-3344服务，通过这个3344获取GitHub的MicroServiceCloud-config仓库中的数据
#获取的是这个仓库master下的dev配置
 

